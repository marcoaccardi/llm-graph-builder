# LLM Graph Builder - Local-Only Configuration
# All configurations are set for local Ollama-only usage

# Embedding Configuration (Local Only)
EMBEDDING_MODEL = "all-MiniLM-L6-v2"  # Local HuggingFace model
RAGAS_EMBEDDING_MODEL = "all-MiniLM-L6-v2"  # Local HuggingFace model for RAGAS
IS_EMBEDDING = "TRUE"

# Ollama Configuration
OLLAMA_BASE_URL = "http://localhost:11434"

# LLM Model Configuration (Ollama Only)
LLM_MODEL_CONFIG_ollama_llama3="llama3.1:8b-instruct-q8_0,http://localhost:11434"

# Graph Processing Configuration
KNN_MIN_SCORE = "0.94"
NUMBER_OF_CHUNKS_TO_COMBINE = 6
UPDATE_GRAPH_CHUNKS_PROCESSED = 20
ENTITY_EMBEDDING="TRUE"
DUPLICATE_SCORE_VALUE =0.97
DUPLICATE_TEXT_DISTANCE =3
MAX_TOKEN_CHUNK_SIZE=2000

# Neo4j Database Configuration
NEO4J_URI = "neo4j://localhost:7687"
NEO4J_USERNAME = "neo4j"
NEO4J_PASSWORD = "password"
NEO4J_DATABASE = "neo4j"

# Local Processing Configuration
GCS_FILE_CACHE = "False" # Local file storage only
ENABLE_USER_AGENT = "False"

# Processing Settings
NEO4J_USER_AGENT=""
YOUTUBE_TRANSCRIPT_PROXY=""
EFFECTIVE_SEARCH_RATIO=5

# Default Models (Local Only)
DEFAULT_DIFFBOT_CHAT_MODEL="ollama_llama3"
GRAPH_CLEANUP_MODEL="ollama_llama3"